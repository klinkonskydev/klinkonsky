---
interface Props {
	heading: string;
}

const { heading } = Astro.props;
---

<section class="pt-24">
  <h4 class="text-3xl font-semibold text-zinc-400 mb-2">
    {heading}<span>.</span>
  </h4>
  <slot />
</section>

<style>
  span { color: rgb(var(--accent)); }
</style>
